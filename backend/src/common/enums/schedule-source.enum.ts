export enum ScheduleSource {
  /** Échéance issue d'un programme de vaccination pré-chargé (Gabon). */
  PROGRAM = 'PROGRAM',
  /** Échéance créée manuellement (soin unique planifié). */
  MANUEL = 'MANUEL',
}